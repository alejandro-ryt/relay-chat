// Todo: Will do some utils for reusable fetch fn...
// type TFetchProps = {
//   endpoint: string;
//   options: Omit<RequestInit, "headers">;
// };

// const fetchInstance = (endpoint: string, options: RequestInit<>) => {
//   return fetch(endpoint, { headers: "", ...options });
// };
